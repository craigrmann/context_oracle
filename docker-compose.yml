services:
  oracle:
    build: .
    ports: ["8000:8000"]
    volumes:
      - oracle_index:/app/.oracle_index
      - /path/to/your/actual/project:/project:ro   # ← your codebase
    environment:
      - ORACLE_ROOT_DIR=/project
      - OPENAI_API_KEY=${OPENAI_API_KEY}   # add to your .env
